services:
  ytcomments:
    build:
      context: ../../
      dockerfile: install/docker/Dockerfile
    container_name: ytcomments_TEST
    ports:
      - "27017:27017"
      - "50051:50051"
    volumes:
      - mongo-data:/data/db
    env_file:
      - ../../.env

volumes:
  mongo-data:
    driver: local